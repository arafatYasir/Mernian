import ProfileHeader from "../components/ProfileHeader";

const ProfilePage = () => {
    return (
        <div className="mt-8">
            <ProfileHeader />
        </div>
    );
};

export default ProfilePage;